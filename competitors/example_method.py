# import numpy as np


def example_method(data, param1 = 10, param2 = 0.5):
    """
    An example method that processes the input data.

    Parameters:
    - data: The input data to be processed.
    - param1: An integer parameter that influences processing (default is 10).
    - param2: A float parameter that influences processing (default is 0.5).

    Returns:
    - result: The processed data.
    """
    # Example processing (to be replaced with actual logic)
    result = data * param1 + param2
    return result
